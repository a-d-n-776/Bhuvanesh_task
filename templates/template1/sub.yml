parameters:
- name: A
- name: B

steps:

    - task: CmdLine@2
      displayName: 'Run a multi-line script'
      inputs:
        script: |

          sub=$((${{ parameters.A }} - ${{ parameters.B }}))
          echo "sub of parameters : $sub"



