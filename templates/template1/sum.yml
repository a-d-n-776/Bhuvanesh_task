parameters:
- name: A
- name: B

steps:

    - task: CmdLine@2
      displayName: 'Run a multi-line script'
      inputs:
        script: |

          
          sum=$((${{ parameters.A }} + ${{ parameters.B }}))
          echo "sum of parameters : $sum"

